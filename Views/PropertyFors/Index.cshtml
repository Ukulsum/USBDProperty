@model IEnumerable<USBDProperty.Models.PropertyFor>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<main id="main">
    <div class="card">
        <div class="card-body">
            <div class="row">
                <div class="col-md-9">
                    <h5 class="card-title">List of Property For</h5>
                </div>
                <div class="col-md-3">
                    <h5 class="card-title"><a asp-action="Create">Create New</a></h5>
                </div>
            </div>


            <table class="table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.PropertyForId)
                        </th>
                          <th>
                            @Html.DisplayNameFor(model => model.PropeFor)
                        </th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.PropertyForId)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.PropeFor)
                            </td>
                            <td>
                                <a asp-action="Edit" asp-route-id="@item.PropertyForId"><i class="bi bi-pencil-square"></i></a> |
                                <a asp-action="Details" asp-route-id="@item.PropertyForId"><i class="bi bi-eye-fill"></i></a> |
                                <a asp-action="Delete" asp-route-id="@item.PropertyForId"><i class="bi bi-trash"></i></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</main>


@*<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PropeFor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PropeFor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PropertyForId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PropertyForId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PropertyForId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
*@